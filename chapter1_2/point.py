class Point:
    def __init__(self, x, y, a, b):
        self.a = a
        self.b = b
        self.c = c
        self.d = d
        if self.y**2 = self.x**3+self.a*x+self.b:
            raise ValueError("is not on the curve")
